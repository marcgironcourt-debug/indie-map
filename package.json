{
  "name": "indie-map",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "dev": "next dev --webpack",
    "build": "prisma generate && next build --webpack",
    "preview": "next build --webpack",
    "start": "next start",
    "lint": "eslint",
    "audit:api-v1": "node -e \"const fs=require('fs');const d=JSON.parse(fs.readFileSync('data/places.json','utf8'));if(!Array.isArray(d))throw new Error('places.json not array');const rs=['id','name','city','country','category','timeZone','createdAt','updatedAt'];const rn=['lat','lng'];const ne=v=>typeof v==='string'&&v.trim().length>0;const num=v=>typeof v==='number'&&Number.isFinite(v);const issues=[];for(let i=0;i<d.length;i++){const x=d[i];if(!x||typeof x!=='object'||Array.isArray(x)){issues.push({i,reason:'not_object'});continue;}for(const k of rs) if(!ne(x[k])) issues.push({i,id:x.id,name:x.name,reason:'missing_'+k});for(const k of rn) if(!num(x[k])) issues.push({i,id:x.id,name:x.name,reason:'invalid_'+k});}fs.writeFileSync('api-v1-audit.json',JSON.stringify({places:d.length,blockingIssues:issues.length,issues},null,2)+'\\n');console.log('places:',d.length);console.log('blocking_issues:',issues.length);if(issues.length){console.log('see api-v1-audit.json');process.exit(2);} \"",
    "submissions:html": "node scripts/submissions-html.mjs | xargs open",
    "submissions:admin": "node scripts/submissions-admin.mjs | xargs open"
  },
  "dependencies": {
    "@prisma/client": "^6.18.0",
    "@vercel/functions": "^3.4.3",
    "maplibre-gl": "^5.16.0",
    "next": "16.1.6",
    "next-intl": "^4.5.0",
    "postcss": "^8.5.6",
    "react": "19.0.1",
    "react-dom": "19.0.1",
    "resend": "^6.9.3",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@types/web-push": "^3.6.4",
    "dotenv": "^17.2.3",
    "eslint": "^9",
    "eslint-config-next": "16.1.6",
    "local-ssl-proxy": "^2.0.5",
    "prisma": "^6.18.0",
    "tailwindcss": "^4",
    "tsx": "^4.20.6",
    "typescript": "^5"
  }
}
