'use client';

export default function ClientMap() {
  return (
    <div className="h-[70vh] w-full grid place-items-center text-sm text-neutral-600 dark:text-neutral-300">
      <div>ClientMap placeholder — Leaflet arrive ensuite.</div>
    </div>
  );
}
